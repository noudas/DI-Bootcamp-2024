
import React from 'react'
const Hello = () => {
  return (
    <>
      <h3>Hello Component</h3>
      <h2>Hello Component</h2>
    </>
  );
};
export default Hello;


/**
 * 1. Create a new React app, e.g., 'my-users'.
 * 2. In the 'src' directory, create a new folder named 'components'.
 * 3. Inside the 'components' folder, create a `User` component.
 * 4. The `User` component should display the following properties:
 *    - Name 
 *    - Email 
 *    - Username 
 *    - An image
 * 5. Render the `User` component on the page.
 * 6. For the image source, use: https://robohash.org/1?size=150x150.
 */